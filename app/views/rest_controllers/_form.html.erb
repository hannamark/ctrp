<%= form_for(@rest_controller) do |f| %>
  <% if @rest_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rest_controller.errors.count, "error") %> prohibited this rest_controller from being saved:</h2>

      <ul>
      <% @rest_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
